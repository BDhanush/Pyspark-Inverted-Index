Libraries to be downloaded to run the inverted_index.py

Python version = python 3.10.6
Spark version = 3.4.0

1. pyspark

Execution:
Run all the cells in the inverted_index.ipynb file to get the outputs.

Note: Avoid changing the path. The path used in the .ipynb file is the relative path. 
Add new text files in the 'text_files' folder relative to the path.